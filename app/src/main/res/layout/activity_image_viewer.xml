<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- Top Bar -->
    <RelativeLayout
        android:id="@+id/top_bar_image_viewer"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="#80000000">

        <TextView
            android:id="@+id/file_name_image_viewer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_marginStart="16dp"
            android:layout_toStartOf="@+id/delete_button_image_viewer"
            android:ellipsize="middle"
            android:singleLine="true"
            android:text="Image File Name"
            android:textColor="@android:color/white"
            android:textSize="18sp" />

        <ImageButton
            android:id="@+id/delete_button_image_viewer"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@+id/close_button_image_viewer"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Delete Image"
            app:srcCompat="@drawable/delete_forever_24px" />

        <ImageButton
            android:id="@+id/close_button_image_viewer"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Close Image Viewer"
            app:srcCompat="@drawable/close_24px" />
    </RelativeLayout>

    <!-- Image Display -->
    <ImageView
        android:id="@+id/image_view_full"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/footer_bar_image_viewer"
        android:layout_below="@id/top_bar_image_viewer"
        android:contentDescription="Full screen image"
        android:scaleType="fitCenter" />

    <!-- Navigation Buttons -->
    <ImageButton
        android:id="@+id/prev_button_image_viewer"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="16dp"
        android:background="#80000000"
        app:srcCompat="@drawable/skip_previous_24px"
        android:contentDescription="Previous Image"
        android:scaleType="fitCenter" />

    <ImageButton
        android:id="@+id/next_button_image_viewer"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="16dp"
        android:background="#80000000"
        app:srcCompat="@drawable/skip_next_24px"
        android:contentDescription="Next Image"
        android:scaleType="fitCenter" />

    <!-- NEW FOOTER -->
    <RelativeLayout
        android:id="@+id/footer_bar_image_viewer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#80000000"
        android:padding="8dp">

        <LinearLayout
            android:id="@+id/footer_buttons_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/open_with_button_image"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="Open With"
                app:srcCompat="@drawable/open_in_new_24px" />

            <ImageButton
                android:id="@+id/share_button_image"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:contentDescription="Share"
                app:srcCompat="@drawable/share_24px" />

        </LinearLayout>

        <!-- DELETION PROGRESS UI (Initially Hidden) -->
        <RelativeLayout
            android:id="@+id/deletion_progress_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignTop="@id/footer_buttons_layout"
            android:layout_alignBottom="@id/footer_buttons_layout"
            android:visibility="gone">

            <ProgressBar
                android:id="@+id/deletion_progress_bar"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_marginEnd="8dp"
                android:layout_toStartOf="@+id/deletion_progress_text"
                android:indeterminate="true" />

            <TextView
                android:id="@+id/deletion_progress_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:text="Deleting..."
                android:textColor="@android:color/white" />
        </RelativeLayout>

    </RelativeLayout>

</RelativeLayout>